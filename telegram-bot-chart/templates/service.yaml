apiVersion: v1
kind: Service
metadata:
  name: {{  .Values.postgres.name  }}
  namespace: {{  .Values.postgres.namespace  }}
    
  selector:
    app: {{  .Values.postgres.name  }}
  ports:
    - port: {{  .Values.postgres.ports.iport}}
      targetPort: {{  .Values.postgres.ports.targetPort}}