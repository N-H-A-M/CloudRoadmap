apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{  .Values.postgres.name  }}-pv
spec:
  capacity:
    storage: {{  .Values.postgres.presistence.capacity  }} 
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: {{  .Values.postgres.presistence.hostPath  }} 